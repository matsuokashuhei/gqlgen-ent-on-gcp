type Schedule implements Node {
  id: ID!
  dayOfWeek: Int!
  startTime: String!
  endTime: String!
  class(time: Time!): Class @goField(forceResolver: true)
  createTime: Time!
  updateTime: Time!
}

enum ScheduleField {
  DAY_OF_WEEK
  START_TIME
}
