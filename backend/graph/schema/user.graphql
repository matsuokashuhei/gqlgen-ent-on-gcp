type User {
  id: ID!
  name: String!
  firebaseUid: String!
  createdAt: Time!
  updatedAt: Time!
}

input CreateUserInput {
  name: String!
  email: String!
  password: String!
}

extend type Mutation {
  createUser(input: CreateUserInput!): User!
}
